# ~/.tmuxinator/login-relay.yml
# you can make as many tabs as you wish...

project_name: Tmuxinator
project_root: ~/
socket_name:  # Not needed.  Remove to use default socket
rvm: 
pre: killall bnac && nac
tabs:
  - editor:
      layout: main-vertical
      panes:
        - hi dev
